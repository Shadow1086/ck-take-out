<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ck.it.mapper.DishMapper">

	<select id="queryPage" resultType="com.ck.it.vo.DishVO">
		SELECT d.* , c.name as categoryName
		FROM dish d
		LEFT JOIN category c ON c.id = d.category_id
		<where>
			<if test="dto.name != null and dto.name != ''">
				and d.name like CONCAT('%',#{dto.name},'%')
			</if>
			<if test="dto.categoryId!=null">
				and d.category_id = #{dto.categoryId}
			</if>
			<if test="dto.status!=null">
				and d.status = #{dto.status}
			</if>
		</where>
		ORDER BY d.create_time desc
	</select>
</mapper>